{
  "name": "buildfest",
  "private": true,
  "description": "Common Table - food resources app. Frontend in frontend/.",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview"
  }
}
